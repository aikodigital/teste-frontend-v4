import React from 'react';
import './Loader.scss'; // Adicione estilos no arquivo CSS

const Loader = () => (
  <div className="loader">
    <div className="spinner"></div>
  </div>
);

export default Loader;