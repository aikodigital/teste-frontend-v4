import React from 'react';
import { useEquipmentContext } from '../../context/EquipmentContext';

const EquipmentList: React.FC = () => {
  const { organizedData, filteredData } = useEquipmentContext();

  return null;
};

export default EquipmentList;
